<%= link_to 'New Movie', "/directors/#{@director.id}/movies/new" %>
<% @movies.each do |movie| %>
  <h3><%= movie.title %></h3>
  <%= link_to "Edit #{movie.title}", "/movies/#{movie.id}/edit" %>
<% end %>
