<h1>Gardens</h1>

<% @gardens.each do |garden| %>
  <h3><%= garden.name %> planted on: <%= garden.created_at%></h3> <%= link_to "Edit #{garden.name}", "/gardens/#{garden.id}/edit" %>
<% end %>
<br>
<br>
<%= link_to "New Garden", '/gardens/new' %>
